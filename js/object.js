//ch4
// an object is a container of properies

const main = () => {

    // {} is linked to Object.prototype
    var empty_object = {}

    var stooge = {
        "first-name": "Jerome",
        "last-name": "Howard"
    }

    console.log(stooge["first-name"])

    // interface is the collection of operations that external code can perform on it
    

}

main()